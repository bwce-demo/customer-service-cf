<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.tibco.bw</groupId>
    <artifactId>CustomerService.parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <relativePath>../CustomerService.parent</relativePath>
  </parent>
  <artifactId>CustomerService</artifactId>
  <packaging>bwear</packaging>
  <properties>
    <pcf.property.file>pcfdev.properties</pcf.property.file>
  </properties>
  <build>
    <plugins>
      <plugin>
        <groupId>com.tibco.plugins</groupId>
        <artifactId>bw6-maven-plugin</artifactId>
        <version>1.2.2</version>
        <extensions>true</extensions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>properties-maven-plugin</artifactId>
        <version>1.0.0</version>
        <executions>
          <execution>
            <phase>initialize</phase>
            <goals>
              <goal>read-project-properties</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <files>
            <file>${pcf.property.file}</file>
          </files>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.cloudfoundry</groupId>
        <artifactId>cf-maven-plugin</artifactId>
        <version>1.1.3</version>
        <configuration>
          <server>${bwpcf.server}</server>
          <target>${bwpcf.target}</target>
          <trustSelfSignedCerts>${bwpcf.trustSelfSignedCerts}</trustSelfSignedCerts>
          <org>${bwpcf.org}</org>
          <space>${bwpcf.space}</space>
          <appname>${bwpcf.appName}</appname>
          <url>${bwpcf.url}</url>
          <instances>${bwpcf.instances}</instances>
          <skip>false</skip>
          <memory>${bwpcf.memory}</memory>
          <buildpack>${bwpcf.buildpack}</buildpack>
          <env>
            <APP_CONFIG_PROFILE>${bwpcf.env.0}</APP_CONFIG_PROFILE>
            <BW_LOGLEVEL>${bwpcf.env.1}</BW_LOGLEVEL>
            <BW_PROFILE>${bwpcf.env.2}</BW_PROFILE>
            <CF_TARGET>${bwpcf.env.3}</CF_TARGET>
          </env>
          <services>
            <service>
              <name>scs-circuit-breaker</name>
              <label>p-circuit-breaker-dashboard</label>
              <version></version>
              <plan>standard</plan>
            </service>
            <service>
              <name>scs-service-registry</name>
              <label>p-service-registry</label>
              <version></version>
              <plan>standard</plan>
            </service>
            <service>
              <name>scs-config-server</name>
              <label>p-config-server</label>
              <version></version>
              <plan>standard</plan>
            </service>
            <service>
              <name>bwce-monitoring</name>
              <label>user-provided</label>
              <version></version>
              <plan></plan>
            </service>
          </services>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
